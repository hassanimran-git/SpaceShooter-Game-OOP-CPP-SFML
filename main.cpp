#include "menu.h"
int main()
{
    // EDIT
    RenderWindow window(VideoMode(780,780), "Space Shooter");
    Menu menu;
    menu.display_menu(window);
    

    // real
    // Menu m;
    // m.display_menu(); 
    return 0;
}